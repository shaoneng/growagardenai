# 重定向规则

# 旧路径重定向
/home / 301
/dashboard /favorites 301

# 确保 www 重定向到非 www（如果配置了自定义域名）
# https://www.growagarden.com/* https://growagarden.com/:splat 301!

# API 路由重定向（如果需要）
# /api/v1/* /api/:splat 301

# API 路由应由 Pages Functions/Next 处理，不要被 SPA 回退拦截
/api/* /api/:splat 200

# SPA 回退 - 必须放在最后（避免匹配 /api/*）
/* /index.html 200
